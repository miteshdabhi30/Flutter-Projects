import 'package:flutter/material.dart';

class DashboardRequestScreen extends StatelessWidget {
  const DashboardRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Request Screen"),
    );
  }
}
